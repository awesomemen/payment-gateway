input {
  tcp {
    port => 5000
    codec => json
  }
}

filter {
  mutate {
    add_field => { "service" => "payment-gateway" }
  }
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "payment-gateway-%{+YYYY.MM.dd}"
  }
  stdout { codec => rubydebug }
}
