任务完成后优先做：\n1. 按改动范围运行最小定向测试，例如 `.\mvnw.cmd --% -q -gs .mvn/global-settings.xml -s .mvn/settings.xml -pl gateway-app -am test -Dtest=PaymentGatewayApplicationTest -Dsurefire.failIfNoSpecifiedTests=false`。\n2. 涉及跨模块、安全、Dubbo、MQ、事务、锁、治理或公共契约时，补充模块级或全仓测试。\n3. 涉及 Docker 运行态时，执行 `docker compose -f docker/local-compose.yml up -d --build gateway-app` 并检查 `/actuator/health`。\n4. 涉及验收页或交付验证时，检查 `/acceptance/index.html` 和 `docs/delivery/` 相关说明。\n5. 最终说明中明确写出实际执行过的命令和未验证项。
